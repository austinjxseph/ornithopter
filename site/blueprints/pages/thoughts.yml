title: Thoughts

options:
  changeSlug: false
  changeStatus: false
  delete: false

columns:
  - width: 1/3
    sections:
      drafts:
        type: pages
        label: Drafts
        status: draft
        template: thought
        sortBy: date desc
        layout: cards
        size: medium
        info: "{{ page.date.toDate('d M Y') }}"

  - width: 2/3
    sections:
      listed:
        type: pages
        label: Published
        status: listed
        template: thought
        sortBy: date desc
        layout: cards
        size: medium
        info: "{{ page.date.toDate('d M Y') }}"
