# Railway Configuration
# This file configures how Railway builds and deploys the service

# Note: Railway will auto-detect the Dockerfile at the repo root
# You can also configure via the Railway dashboard for more control

# Environment variables:
# - KIRBY_DEBUG=false
